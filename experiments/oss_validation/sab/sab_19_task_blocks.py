# --------------------------------------------------------------------------- #
# SAB lightweight agent-eval representatives (L1 wired, mirrors sab.h_importances_92).
# Each: data + official gold solve.py / run_eval.py materialised as a sentinel at
# data/oss/<id>/; the generalised runner symlinks per-task datasets from the
# read-only /repo vendor corpus at launch and re-executes solve.py (gold-as-solve,
# standing in for an agent's program) then grades with run_eval.py.
# NOTE: the sandbox image ships only numpy/pandas/scikit-learn/scipy, so tasks
# whose gold/eval programs need a domain lib (rdkit/neurokit2/biopsykit/ccobra/
# matminer/geopandas/MDAnalysis/cftime+iris) are L1 env-blocked -> the launch still
# completes honestly (passed=False + port_notes naming the missing module).
# --------------------------------------------------------------------------- #
    BenchmarkTask(
        task_id="sab.mat_feature_select_2",
        name='SAB #2 · Mat Feature Select (agent-eval rep)',
        source_project="OSU-NLP-Group/ScienceAgentBench",
        category="agent_eval",
        modality="mixed",
        dataset_desc='ScienceAgentBench 实例 #2（Computational Chemistry）：由官方 gold program 计算目标并写入 pred_results/mat_diffusion_features.csv，官方 run_eval.py 在容差 1e-4 内比对 gold 参考。数据集 .npy/.csv 运行时从 /repo vendor 软链，无需复制 3.7GB 语料。',
        eval_metric="max_abs_error",
        direction="lower",
        baseline=None,
        reference=None,
        gates={"tolerance": 1e-4},
        harness="sab_eval",
        run_command="python /repo/scripts/sandbox_examples/run_sab_sandbox.py --data-dir /data/ --task sab.mat_feature_select_2",
        source_path=_oss("benchmark_tasks/suites/data/vendor/ScienceAgentBench/benchmark/gold_programs/mat_feature_select.py"),
        tags=['science-discovery', 'agent-eval', 'computational', 'mastml'],
        data_local=True,
        eval_method='平台研究方式：solve.py（gold-as-solve，代表 agent 产出程序）在沙箱内重跑，run_eval.py 比对 gold；指标 max_abs_error<=1e-4 即通过。数据集从 /repo vendor 软链，避免复制 3.7GB。',
        note='L1 env-blocked：gold/eval 程序需要 mastml（不在 numpy/pandas/scikit-learn/scipy 沙箱镜像中）；launch 仍以 passed=False + port_notes 指明缺失模块的方式诚实完成。data_local=True（sentinel 在 data/oss/sab.mat_feature_select_2/；重数据留在 /repo vendor 只读挂载）。',
    ),
    BenchmarkTask(
        task_id="sab.predict_bulk_modulus_3",
        name='SAB #3 · Predict Bulk Modulus (agent-eval rep)',
        source_project="OSU-NLP-Group/ScienceAgentBench",
        category="agent_eval",
        modality="mixed",
        dataset_desc='ScienceAgentBench 实例 #3（Computational Chemistry）：由官方 gold program 计算目标并写入 pred_results/compound_bulk_modulus.csv，官方 run_eval.py 在容差 1e-4 内比对 gold 参考。数据集 .npy/.csv 运行时从 /repo vendor 软链，无需复制 3.7GB 语料。',
        eval_metric="max_abs_error",
        direction="lower",
        baseline=None,
        reference=None,
        gates={"tolerance": 1e-4},
        harness="sab_eval",
        run_command="python /repo/scripts/sandbox_examples/run_sab_sandbox.py --data-dir /data/ --task sab.predict_bulk_modulus_3",
        source_path=_oss("benchmark_tasks/suites/data/vendor/ScienceAgentBench/benchmark/gold_programs/predict_bulk_modulus.py"),
        tags=['science-discovery', 'agent-eval', 'computational', 'matminer'],
        data_local=True,
        eval_method='平台研究方式：solve.py（gold-as-solve，代表 agent 产出程序）在沙箱内重跑，run_eval.py 比对 gold；指标 max_abs_error<=1e-4 即通过。数据集从 /repo vendor 软链，避免复制 3.7GB。',
        note='L1 env-blocked：gold/eval 程序需要 matminer（不在 numpy/pandas/scikit-learn/scipy 沙箱镜像中）；launch 仍以 passed=False + port_notes 指明缺失模块的方式诚实完成。data_local=True（sentinel 在 data/oss/sab.predict_bulk_modulus_3/；重数据留在 /repo vendor 只读挂载）。',
    ),
    BenchmarkTask(
        task_id="sab.dili_models_ecfp_rf_18",
        name='SAB #18 · Dili Models Ecfp Rf (agent-eval rep)',
        source_project="OSU-NLP-Group/ScienceAgentBench",
        category="agent_eval",
        modality="mixed",
        dataset_desc='ScienceAgentBench 实例 #18（Bioinformatics）：由官方 gold program 计算目标并写入 pred_results/MCNC_RF.csv，官方 run_eval.py 在容差 1e-4 内比对 gold 参考。数据集 .npy/.csv 运行时从 /repo vendor 软链，无需复制 3.7GB 语料。',
        eval_metric="max_abs_error",
        direction="lower",
        baseline=None,
        reference=None,
        gates={"tolerance": 1e-4},
        harness="sab_eval",
        run_command="python /repo/scripts/sandbox_examples/run_sab_sandbox.py --data-dir /data/ --task sab.dili_models_ecfp_rf_18",
        source_path=_oss("benchmark_tasks/suites/data/vendor/ScienceAgentBench/benchmark/gold_programs/DILI_models_ECFP_RF.py"),
        tags=['science-discovery', 'agent-eval', 'bioinformatics', 'rdkit'],
        data_local=True,
        eval_method='平台研究方式：solve.py（gold-as-solve，代表 agent 产出程序）在沙箱内重跑，run_eval.py 比对 gold；指标 max_abs_error<=1e-4 即通过。数据集从 /repo vendor 软链，避免复制 3.7GB。',
        note='L1 env-blocked：gold/eval 程序需要 rdkit（不在 numpy/pandas/scikit-learn/scipy 沙箱镜像中）；launch 仍以 passed=False + port_notes 指明缺失模块的方式诚实完成。data_local=True（sentinel 在 data/oss/sab.dili_models_ecfp_rf_18/；重数据留在 /repo vendor 只读挂载）。',
    ),
    BenchmarkTask(
        task_id="sab.dili_models_ecfp_svm_19",
        name='SAB #19 · Dili Models Ecfp Svm (agent-eval rep)',
        source_project="OSU-NLP-Group/ScienceAgentBench",
        category="agent_eval",
        modality="mixed",
        dataset_desc='ScienceAgentBench 实例 #19（Bioinformatics）：由官方 gold program 计算目标并写入 pred_results/MCNC_SVM.csv，官方 run_eval.py 在容差 1e-4 内比对 gold 参考。数据集 .npy/.csv 运行时从 /repo vendor 软链，无需复制 3.7GB 语料。',
        eval_metric="max_abs_error",
        direction="lower",
        baseline=None,
        reference=None,
        gates={"tolerance": 1e-4},
        harness="sab_eval",
        run_command="python /repo/scripts/sandbox_examples/run_sab_sandbox.py --data-dir /data/ --task sab.dili_models_ecfp_svm_19",
        source_path=_oss("benchmark_tasks/suites/data/vendor/ScienceAgentBench/benchmark/gold_programs/DILI_models_ECFP_SVM.py"),
        tags=['science-discovery', 'agent-eval', 'bioinformatics', 'rdkit'],
        data_local=True,
        eval_method='平台研究方式：solve.py（gold-as-solve，代表 agent 产出程序）在沙箱内重跑，run_eval.py 比对 gold；指标 max_abs_error<=1e-4 即通过。数据集从 /repo vendor 软链，避免复制 3.7GB。',
        note='L1 env-blocked：gold/eval 程序需要 rdkit（不在 numpy/pandas/scikit-learn/scipy 沙箱镜像中）；launch 仍以 passed=False + port_notes 指明缺失模块的方式诚实完成。data_local=True（sentinel 在 data/oss/sab.dili_models_ecfp_svm_19/；重数据留在 /repo vendor 只读挂载）。',
    ),
    BenchmarkTask(
        task_id="sab.deforestation_21",
        name='SAB #21 · Deforestation (agent-eval rep)',
        source_project="OSU-NLP-Group/ScienceAgentBench",
        category="agent_eval",
        modality="mixed",
        dataset_desc='ScienceAgentBench 实例 #21（Geographical Information Science）：由官方 gold program 计算目标并写入 pred_results/deforestation_rate.csv，官方 run_eval.py 在容差 1e-4 内比对 gold 参考。数据集 .npy/.csv 运行时从 /repo vendor 软链，无需复制 3.7GB 语料。',
        eval_metric="max_abs_error",
        direction="lower",
        baseline=None,
        reference=None,
        gates={"tolerance": 1e-4},
        harness="sab_eval",
        run_command="python /repo/scripts/sandbox_examples/run_sab_sandbox.py --data-dir /data/ --task sab.deforestation_21",
        source_path=_oss("benchmark_tasks/suites/data/vendor/ScienceAgentBench/benchmark/gold_programs/deforestation.py"),
        tags=['science-discovery', 'agent-eval', 'geographical', 'geopandas'],
        data_local=True,
        eval_method='平台研究方式：solve.py（gold-as-solve，代表 agent 产出程序）在沙箱内重跑，run_eval.py 比对 gold；指标 max_abs_error<=1e-4 即通过。数据集从 /repo vendor 软链，避免复制 3.7GB。',
        note='L1 env-blocked：gold/eval 程序需要 geopandas（不在 numpy/pandas/scikit-learn/scipy 沙箱镜像中）；launch 仍以 passed=False + port_notes 指明缺失模块的方式诚实完成。data_local=True（sentinel 在 data/oss/sab.deforestation_21/；重数据留在 /repo vendor 只读挂载）。',
    ),
    BenchmarkTask(
        task_id="sab.ligand_fingerprint_26",
        name='SAB #26 · Ligand Fingerprint (agent-eval rep)',
        source_project="OSU-NLP-Group/ScienceAgentBench",
        category="agent_eval",
        modality="mixed",
        dataset_desc='ScienceAgentBench 实例 #26（Computational Chemistry）：由官方 gold program 计算目标并写入 pred_results/ligand_fingerprint_pred.csv，官方 run_eval.py 在容差 1e-4 内比对 gold 参考。数据集 .npy/.csv 运行时从 /repo vendor 软链，无需复制 3.7GB 语料。',
        eval_metric="max_abs_error",
        direction="lower",
        baseline=None,
        reference=None,
        gates={"tolerance": 1e-4},
        harness="sab_eval",
        run_command="python /repo/scripts/sandbox_examples/run_sab_sandbox.py --data-dir /data/ --task sab.ligand_fingerprint_26",
        source_path=_oss("benchmark_tasks/suites/data/vendor/ScienceAgentBench/benchmark/gold_programs/ligand_fingerprint.py"),
        tags=['science-discovery', 'agent-eval', 'computational', 'MDAnalysis'],
        data_local=True,
        eval_method='平台研究方式：solve.py（gold-as-solve，代表 agent 产出程序）在沙箱内重跑，run_eval.py 比对 gold；指标 max_abs_error<=1e-4 即通过。数据集从 /repo vendor 软链，避免复制 3.7GB。',
        note='L1 env-blocked：gold/eval 程序需要 MDAnalysis, prolif（不在 numpy/pandas/scikit-learn/scipy 沙箱镜像中）；launch 仍以 passed=False + port_notes 指明缺失模块的方式诚实完成。data_local=True（sentinel 在 data/oss/sab.ligand_fingerprint_26/；重数据留在 /repo vendor 只读挂载）。',
    ),
    BenchmarkTask(
        task_id="sab.bio_eventrelated_analyze_29",
        name='SAB #29 · Bio Eventrelated Analyze (agent-eval rep)',
        source_project="OSU-NLP-Group/ScienceAgentBench",
        category="agent_eval",
        modality="mixed",
        dataset_desc='ScienceAgentBench 实例 #29（Psychology and Cognitive science）：由官方 gold program 计算目标并写入 pred_results/bio_eventrelated_100hz_analysis_pred.csv，官方 run_eval.py 在容差 1e-4 内比对 gold 参考。数据集 .npy/.csv 运行时从 /repo vendor 软链，无需复制 3.7GB 语料。',
        eval_metric="max_abs_error",
        direction="lower",
        baseline=None,
        reference=None,
        gates={"tolerance": 1e-4},
        harness="sab_eval",
        run_command="python /repo/scripts/sandbox_examples/run_sab_sandbox.py --data-dir /data/ --task sab.bio_eventrelated_analyze_29",
        source_path=_oss("benchmark_tasks/suites/data/vendor/ScienceAgentBench/benchmark/gold_programs/bio_eventrelated_analyze.py"),
        tags=['science-discovery', 'agent-eval', 'psychology', 'neurokit2'],
        data_local=True,
        eval_method='平台研究方式：solve.py（gold-as-solve，代表 agent 产出程序）在沙箱内重跑，run_eval.py 比对 gold；指标 max_abs_error<=1e-4 即通过。数据集从 /repo vendor 软链，避免复制 3.7GB。',
        note='L1 env-blocked：gold/eval 程序需要 neurokit2（不在 numpy/pandas/scikit-learn/scipy 沙箱镜像中）；launch 仍以 passed=False + port_notes 指明缺失模块的方式诚实完成。data_local=True（sentinel 在 data/oss/sab.bio_eventrelated_analyze_29/；重数据留在 /repo vendor 只读挂载）。',
    ),
    BenchmarkTask(
        task_id="sab.hrv_analyze_34",
        name='SAB #34 · Hrv Analyze (agent-eval rep)',
        source_project="OSU-NLP-Group/ScienceAgentBench",
        category="agent_eval",
        modality="mixed",
        dataset_desc='ScienceAgentBench 实例 #34（Psychology and Cognitive science）：由官方 gold program 计算目标并写入 pred_results/hrv_analysis_pred.csv，官方 run_eval.py 在容差 1e-4 内比对 gold 参考。数据集 .npy/.csv 运行时从 /repo vendor 软链，无需复制 3.7GB 语料。',
        eval_metric="max_abs_error",
        direction="lower",
        baseline=None,
        reference=None,
        gates={"tolerance": 1e-4},
        harness="sab_eval",
        run_command="python /repo/scripts/sandbox_examples/run_sab_sandbox.py --data-dir /data/ --task sab.hrv_analyze_34",
        source_path=_oss("benchmark_tasks/suites/data/vendor/ScienceAgentBench/benchmark/gold_programs/HRV_analyze.py"),
        tags=['science-discovery', 'agent-eval', 'psychology', 'neurokit2'],
        data_local=True,
        eval_method='平台研究方式：solve.py（gold-as-solve，代表 agent 产出程序）在沙箱内重跑，run_eval.py 比对 gold；指标 max_abs_error<=1e-4 即通过。数据集从 /repo vendor 软链，避免复制 3.7GB。',
        note='L1 env-blocked：gold/eval 程序需要 neurokit2（不在 numpy/pandas/scikit-learn/scipy 沙箱镜像中）；launch 仍以 passed=False + port_notes 指明缺失模块的方式诚实完成。data_local=True（sentinel 在 data/oss/sab.hrv_analyze_34/；重数据留在 /repo vendor 只读挂载）。',
    ),
    BenchmarkTask(
        task_id="sab.rrv_analyze_35",
        name='SAB #35 · Rrv Analyze (agent-eval rep)',
        source_project="OSU-NLP-Group/ScienceAgentBench",
        category="agent_eval",
        modality="mixed",
        dataset_desc='ScienceAgentBench 实例 #35（Psychology and Cognitive science）：由官方 gold program 计算目标并写入 pred_results/rrv_analysis_pred.csv，官方 run_eval.py 在容差 1e-4 内比对 gold 参考。数据集 .npy/.csv 运行时从 /repo vendor 软链，无需复制 3.7GB 语料。',
        eval_metric="max_abs_error",
        direction="lower",
        baseline=None,
        reference=None,
        gates={"tolerance": 1e-4},
        harness="sab_eval",
        run_command="python /repo/scripts/sandbox_examples/run_sab_sandbox.py --data-dir /data/ --task sab.rrv_analyze_35",
        source_path=_oss("benchmark_tasks/suites/data/vendor/ScienceAgentBench/benchmark/gold_programs/RRV_analyze.py"),
        tags=['science-discovery', 'agent-eval', 'psychology', 'neurokit2'],
        data_local=True,
        eval_method='平台研究方式：solve.py（gold-as-solve，代表 agent 产出程序）在沙箱内重跑，run_eval.py 比对 gold；指标 max_abs_error<=1e-4 即通过。数据集从 /repo vendor 软链，避免复制 3.7GB。',
        note='L1 env-blocked：gold/eval 程序需要 neurokit2（不在 numpy/pandas/scikit-learn/scipy 沙箱镜像中）；launch 仍以 passed=False + port_notes 指明缺失模块的方式诚实完成。data_local=True（sentinel 在 data/oss/sab.rrv_analyze_35/；重数据留在 /repo vendor 只读挂载）。',
    ),
    BenchmarkTask(
        task_id="sab.cft_37",
        name='SAB #37 · Cft (agent-eval rep)',
        source_project="OSU-NLP-Group/ScienceAgentBench",
        category="agent_eval",
        modality="mixed",
        dataset_desc='ScienceAgentBench 实例 #37（Psychology and Cognitive science）：由官方 gold program 计算目标并写入 pred_results/cft_pred_results.json，官方 run_eval.py 在容差 1e-4 内比对 gold 参考。数据集 .npy/.csv 运行时从 /repo vendor 软链，无需复制 3.7GB 语料。',
        eval_metric="max_abs_error",
        direction="lower",
        baseline=None,
        reference=None,
        gates={"tolerance": 1e-4},
        harness="sab_eval",
        run_command="python /repo/scripts/sandbox_examples/run_sab_sandbox.py --data-dir /data/ --task sab.cft_37",
        source_path=_oss("benchmark_tasks/suites/data/vendor/ScienceAgentBench/benchmark/gold_programs/cft.py"),
        tags=['science-discovery', 'agent-eval', 'psychology', 'biopsykit'],
        data_local=True,
        eval_method='平台研究方式：solve.py（gold-as-solve，代表 agent 产出程序）在沙箱内重跑，run_eval.py 比对 gold；指标 max_abs_error<=1e-4 即通过。数据集从 /repo vendor 软链，避免复制 3.7GB。',
        note='L1 env-blocked：gold/eval 程序需要 biopsykit（不在 numpy/pandas/scikit-learn/scipy 沙箱镜像中）；launch 仍以 passed=False + port_notes 指明缺失模块的方式诚实完成。data_local=True（sentinel 在 data/oss/sab.cft_37/；重数据留在 /repo vendor 只读挂载）。',
    ),
    BenchmarkTask(
        task_id="sab.md_rf_40",
        name='SAB #40 · Md Rf (agent-eval rep)',
        source_project="OSU-NLP-Group/ScienceAgentBench",
        category="agent_eval",
        modality="mixed",
        dataset_desc='ScienceAgentBench 实例 #40（Bioinformatics）：由官方 gold program 计算目标并写入 pred_results/MD_MCNC_RF.csv，官方 run_eval.py 在容差 1e-4 内比对 gold 参考。数据集 .npy/.csv 运行时从 /repo vendor 软链，无需复制 3.7GB 语料。',
        eval_metric="max_abs_error",
        direction="lower",
        baseline=None,
        reference=None,
        gates={"tolerance": 1e-4},
        harness="sab_eval",
        run_command="python /repo/scripts/sandbox_examples/run_sab_sandbox.py --data-dir /data/ --task sab.md_rf_40",
        source_path=_oss("benchmark_tasks/suites/data/vendor/ScienceAgentBench/benchmark/gold_programs/MD_RF.py"),
        tags=['science-discovery', 'agent-eval', 'bioinformatics', 'rdkit'],
        data_local=True,
        eval_method='平台研究方式：solve.py（gold-as-solve，代表 agent 产出程序）在沙箱内重跑，run_eval.py 比对 gold；指标 max_abs_error<=1e-4 即通过。数据集从 /repo vendor 软链，避免复制 3.7GB。',
        note='L1 env-blocked：gold/eval 程序需要 rdkit（不在 numpy/pandas/scikit-learn/scipy 沙箱镜像中）；launch 仍以 passed=False + port_notes 指明缺失模块的方式诚实完成。data_local=True（sentinel 在 data/oss/sab.md_rf_40/；重数据留在 /repo vendor 只读挂载）。',
    ),
    BenchmarkTask(
        task_id="sab.md_knn_41",
        name='SAB #41 · Md Knn (agent-eval rep)',
        source_project="OSU-NLP-Group/ScienceAgentBench",
        category="agent_eval",
        modality="mixed",
        dataset_desc='ScienceAgentBench 实例 #41（Bioinformatics）：由官方 gold program 计算目标并写入 pred_results/MD_MCNC_KNN.csv，官方 run_eval.py 在容差 1e-4 内比对 gold 参考。数据集 .npy/.csv 运行时从 /repo vendor 软链，无需复制 3.7GB 语料。',
        eval_metric="max_abs_error",
        direction="lower",
        baseline=None,
        reference=None,
        gates={"tolerance": 1e-4},
        harness="sab_eval",
        run_command="python /repo/scripts/sandbox_examples/run_sab_sandbox.py --data-dir /data/ --task sab.md_knn_41",
        source_path=_oss("benchmark_tasks/suites/data/vendor/ScienceAgentBench/benchmark/gold_programs/MD_KNN.py"),
        tags=['science-discovery', 'agent-eval', 'bioinformatics', 'rdkit'],
        data_local=True,
        eval_method='平台研究方式：solve.py（gold-as-solve，代表 agent 产出程序）在沙箱内重跑，run_eval.py 比对 gold；指标 max_abs_error<=1e-4 即通过。数据集从 /repo vendor 软链，避免复制 3.7GB。',
        note='L1 env-blocked：gold/eval 程序需要 rdkit（不在 numpy/pandas/scikit-learn/scipy 沙箱镜像中）；launch 仍以 passed=False + port_notes 指明缺失模块的方式诚实完成。data_local=True（sentinel 在 data/oss/sab.md_knn_41/；重数据留在 /repo vendor 只读挂载）。',
    ),
    BenchmarkTask(
        task_id="sab.imu_44",
        name='SAB #44 · Imu (agent-eval rep)',
        source_project="OSU-NLP-Group/ScienceAgentBench",
        category="agent_eval",
        modality="mixed",
        dataset_desc='ScienceAgentBench 实例 #44（Psychology and Cognitive science）：由官方 gold program 计算目标并写入 pred_results/imu_pred.json，官方 run_eval.py 在容差 1e-4 内比对 gold 参考。数据集 .npy/.csv 运行时从 /repo vendor 软链，无需复制 3.7GB 语料。',
        eval_metric="max_abs_error",
        direction="lower",
        baseline=None,
        reference=None,
        gates={"tolerance": 1e-4},
        harness="sab_eval",
        run_command="python /repo/scripts/sandbox_examples/run_sab_sandbox.py --data-dir /data/ --task sab.imu_44",
        source_path=_oss("benchmark_tasks/suites/data/vendor/ScienceAgentBench/benchmark/gold_programs/imu.py"),
        tags=['science-discovery', 'agent-eval', 'psychology', 'biopsykit'],
        data_local=True,
        eval_method='平台研究方式：solve.py（gold-as-solve，代表 agent 产出程序）在沙箱内重跑，run_eval.py 比对 gold；指标 max_abs_error<=1e-4 即通过。数据集从 /repo vendor 软链，避免复制 3.7GB。',
        note='L1 env-blocked：gold/eval 程序需要 biopsykit（不在 numpy/pandas/scikit-learn/scipy 沙箱镜像中）；launch 仍以 passed=False + port_notes 指明缺失模块的方式诚实完成。data_local=True（sentinel 在 data/oss/sab.imu_44/；重数据留在 /repo vendor 只读挂载）。',
    ),
    BenchmarkTask(
        task_id="sab.questionnaire_45",
        name='SAB #45 · Questionnaire (agent-eval rep)',
        source_project="OSU-NLP-Group/ScienceAgentBench",
        category="agent_eval",
        modality="mixed",
        dataset_desc='ScienceAgentBench 实例 #45（Psychology and Cognitive science）：由官方 gold program 计算目标并写入 pred_results/questionnaire_pred.csv，官方 run_eval.py 在容差 1e-4 内比对 gold 参考。数据集 .npy/.csv 运行时从 /repo vendor 软链，无需复制 3.7GB 语料。',
        eval_metric="max_abs_error",
        direction="lower",
        baseline=None,
        reference=None,
        gates={"tolerance": 1e-4},
        harness="sab_eval",
        run_command="python /repo/scripts/sandbox_examples/run_sab_sandbox.py --data-dir /data/ --task sab.questionnaire_45",
        source_path=_oss("benchmark_tasks/suites/data/vendor/ScienceAgentBench/benchmark/gold_programs/questionnaire.py"),
        tags=['science-discovery', 'agent-eval', 'psychology', 'biopsykit'],
        data_local=True,
        eval_method='平台研究方式：solve.py（gold-as-solve，代表 agent 产出程序）在沙箱内重跑，run_eval.py 比对 gold；指标 max_abs_error<=1e-4 即通过。数据集从 /repo vendor 软链，避免复制 3.7GB。',
        note='L1 env-blocked：gold/eval 程序需要 biopsykit（不在 numpy/pandas/scikit-learn/scipy 沙箱镜像中）；launch 仍以 passed=False + port_notes 指明缺失模块的方式诚实完成。data_local=True（sentinel 在 data/oss/sab.questionnaire_45/；重数据留在 /repo vendor 只读挂载）。',
    ),
    BenchmarkTask(
        task_id="sab.nvc_gen_ind_58",
        name='SAB #58 · Nvc Gen Ind (agent-eval rep)',
        source_project="OSU-NLP-Group/ScienceAgentBench",
        category="agent_eval",
        modality="mixed",
        dataset_desc='ScienceAgentBench 实例 #58（Psychology and Cognitive science）：由官方 gold program 计算目标并写入 pred_results/individuals.csv，官方 run_eval.py 在容差 1e-4 内比对 gold 参考。数据集 .npy/.csv 运行时从 /repo vendor 软链，无需复制 3.7GB 语料。',
        eval_metric="max_abs_error",
        direction="lower",
        baseline=None,
        reference=None,
        gates={"tolerance": 1e-4},
        harness="sab_eval",
        run_command="python /repo/scripts/sandbox_examples/run_sab_sandbox.py --data-dir /data/ --task sab.nvc_gen_ind_58",
        source_path=_oss("benchmark_tasks/suites/data/vendor/ScienceAgentBench/benchmark/gold_programs/nvc_gen_ind.py"),
        tags=['science-discovery', 'agent-eval', 'psychology', 'ccobra'],
        data_local=True,
        eval_method='平台研究方式：solve.py（gold-as-solve，代表 agent 产出程序）在沙箱内重跑，run_eval.py 比对 gold；指标 max_abs_error<=1e-4 即通过。数据集从 /repo vendor 软链，避免复制 3.7GB。',
        note='L1 env-blocked：gold/eval 程序需要 ccobra（不在 numpy/pandas/scikit-learn/scipy 沙箱镜像中）；launch 仍以 passed=False + port_notes 指明缺失模块的方式诚实完成。data_local=True（sentinel 在 data/oss/sab.nvc_gen_ind_58/；重数据留在 /repo vendor 只读挂载）。',
    ),
    BenchmarkTask(
        task_id="sab.nvc_accuracies_60",
        name='SAB #60 · Nvc Accuracies (agent-eval rep)',
        source_project="OSU-NLP-Group/ScienceAgentBench",
        category="agent_eval",
        modality="mixed",
        dataset_desc='ScienceAgentBench 实例 #60（Psychology and Cognitive science）：由官方 gold program 计算目标并写入 pred_results/accuracies.csv，官方 run_eval.py 在容差 1e-4 内比对 gold 参考。数据集 .npy/.csv 运行时从 /repo vendor 软链，无需复制 3.7GB 语料。',
        eval_metric="max_abs_error",
        direction="lower",
        baseline=None,
        reference=None,
        gates={"tolerance": 1e-4},
        harness="sab_eval",
        run_command="python /repo/scripts/sandbox_examples/run_sab_sandbox.py --data-dir /data/ --task sab.nvc_accuracies_60",
        source_path=_oss("benchmark_tasks/suites/data/vendor/ScienceAgentBench/benchmark/gold_programs/nvc_accuracies.py"),
        tags=['science-discovery', 'agent-eval', 'psychology', 'ccobra'],
        data_local=True,
        eval_method='平台研究方式：solve.py（gold-as-solve，代表 agent 产出程序）在沙箱内重跑，run_eval.py 比对 gold；指标 max_abs_error<=1e-4 即通过。数据集从 /repo vendor 软链，避免复制 3.7GB。',
        note='L1 env-blocked：gold/eval 程序需要 ccobra（不在 numpy/pandas/scikit-learn/scipy 沙箱镜像中）；launch 仍以 passed=False + port_notes 指明缺失模块的方式诚实完成。data_local=True（sentinel 在 data/oss/sab.nvc_accuracies_60/；重数据留在 /repo vendor 只读挂载）。',
    ),
    BenchmarkTask(
        task_id="sab.cogsci_pattern_high_sim_67",
        name='SAB #67 · Cogsci Pattern High Sim (agent-eval rep)',
        source_project="OSU-NLP-Group/ScienceAgentBench",
        category="agent_eval",
        modality="mixed",
        dataset_desc='ScienceAgentBench 实例 #67（Psychology and Cognitive science）：由官方 gold program 计算目标并写入 pred_results/CogSci_pattern_high_sim_data_pred.csv，官方 run_eval.py 在容差 1e-4 内比对 gold 参考。数据集 .npy/.csv 运行时从 /repo vendor 软链，无需复制 3.7GB 语料。',
        eval_metric="max_abs_error",
        direction="lower",
        baseline=None,
        reference=None,
        gates={"tolerance": 1e-4},
        harness="sab_eval",
        run_command="python /repo/scripts/sandbox_examples/run_sab_sandbox.py --data-dir /data/ --task sab.cogsci_pattern_high_sim_67",
        source_path=_oss("benchmark_tasks/suites/data/vendor/ScienceAgentBench/benchmark/gold_programs/CogSci_pattern_high_sim.py"),
        tags=['science-discovery', 'agent-eval', 'psychology', 'ccobra'],
        data_local=True,
        eval_method='平台研究方式：solve.py（gold-as-solve，代表 agent 产出程序）在沙箱内重跑，run_eval.py 比对 gold；指标 max_abs_error<=1e-4 即通过。数据集从 /repo vendor 软链，避免复制 3.7GB。',
        note='L1 env-blocked：gold/eval 程序需要 ccobra（不在 numpy/pandas/scikit-learn/scipy 沙箱镜像中）；launch 仍以 passed=False + port_notes 指明缺失模块的方式诚实完成。data_local=True（sentinel 在 data/oss/sab.cogsci_pattern_high_sim_67/；重数据留在 /repo vendor 只读挂载）。',
    ),
    BenchmarkTask(
        task_id="sab.saliva_85",
        name='SAB #85 · Saliva (agent-eval rep)',
        source_project="OSU-NLP-Group/ScienceAgentBench",
        category="agent_eval",
        modality="mixed",
        dataset_desc='ScienceAgentBench 实例 #85（Bioinformatics）：由官方 gold program 计算目标并写入 pred_results/saliva_pred.json，官方 run_eval.py 在容差 1e-4 内比对 gold 参考。数据集 .npy/.csv 运行时从 /repo vendor 软链，无需复制 3.7GB 语料。',
        eval_metric="max_abs_error",
        direction="lower",
        baseline=None,
        reference=None,
        gates={"tolerance": 1e-4},
        harness="sab_eval",
        run_command="python /repo/scripts/sandbox_examples/run_sab_sandbox.py --data-dir /data/ --task sab.saliva_85",
        source_path=_oss("benchmark_tasks/suites/data/vendor/ScienceAgentBench/benchmark/gold_programs/saliva.py"),
        tags=['science-discovery', 'agent-eval', 'bioinformatics', 'biopsykit'],
        data_local=True,
        eval_method='平台研究方式：solve.py（gold-as-solve，代表 agent 产出程序）在沙箱内重跑，run_eval.py 比对 gold；指标 max_abs_error<=1e-4 即通过。数据集从 /repo vendor 软链，避免复制 3.7GB。',
        note='L1 env-blocked：gold/eval 程序需要 biopsykit（不在 numpy/pandas/scikit-learn/scipy 沙箱镜像中）；launch 仍以 passed=False + port_notes 指明缺失模块的方式诚实完成。data_local=True（sentinel 在 data/oss/sab.saliva_85/；重数据留在 /repo vendor 只读挂载）。',
    ),
    BenchmarkTask(
        task_id="sab.polynomial_fit_87",
        name='SAB #87 · Polynomial Fit (agent-eval rep)',
        source_project="OSU-NLP-Group/ScienceAgentBench",
        category="agent_eval",
        modality="mixed",
        dataset_desc='ScienceAgentBench 实例 #87（Geographical Information Science）：由官方 gold program 计算目标并写入 pred_results/polynomial_fit_pred.csv，官方 run_eval.py 在容差 1e-4 内比对 gold 参考。数据集 .npy/.csv 运行时从 /repo vendor 软链，无需复制 3.7GB 语料。',
        eval_metric="max_abs_error",
        direction="lower",
        baseline=None,
        reference=None,
        gates={"tolerance": 1e-4},
        harness="sab_eval",
        run_command="python /repo/scripts/sandbox_examples/run_sab_sandbox.py --data-dir /data/ --task sab.polynomial_fit_87",
        source_path=_oss("benchmark_tasks/suites/data/vendor/ScienceAgentBench/benchmark/gold_programs/polynomial_fit.py"),
        tags=['science-discovery', 'agent-eval', 'geographical', 'cftime'],
        data_local=True,
        eval_method='平台研究方式：solve.py（gold-as-solve，代表 agent 产出程序）在沙箱内重跑，run_eval.py 比对 gold；指标 max_abs_error<=1e-4 即通过。数据集从 /repo vendor 软链，避免复制 3.7GB。',
        note='L1 env-blocked：gold/eval 程序需要 cftime, iris（不在 numpy/pandas/scikit-learn/scipy 沙箱镜像中）；launch 仍以 passed=False + port_notes 指明缺失模块的方式诚实完成。data_local=True（sentinel 在 data/oss/sab.polynomial_fit_87/；重数据留在 /repo vendor 只读挂载）。',
    ),